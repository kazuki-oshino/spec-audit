{
  "name": "specaudit",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "build:bridge": "tsc -p bridge/tsconfig.json",
    "test:bridge": "npm run build:bridge && node --test bridge/dist/test/*.test.js"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.156.1",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "typescript": "6.0.3"
  }
}
